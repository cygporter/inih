name: Prepare release
on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: Name for the release tag
        required: false
        default: ''
        type: string
permissions:
  contents: write
jobs:
  prep-release:
    uses: me-and/cygporter/.github/workflows/prep-release.yml@workflow
    with:
      cygport_file: libinih.cygport
      release_tag: ${{ inputs.release_tag }}
